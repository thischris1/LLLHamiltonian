name: Intel OneAPI build

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  build_intel_champ_fparser:
    runs-on: ubuntu-20.04
    defaults:
      run:
        shell: bash --noprofile --norc {0}
    steps:
    - uses: actions/checkout@v2
    - name: setup repo
      run: |
        wget https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB
        sudo apt-key add GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB
        rm GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB
        sudo echo "deb https://apt.repos.intel.com/oneapi all main" | sudo tee /etc/apt/sources.list.d/oneAPI.list
        sudo apt-get update
    - name: install
      run: |
        sudo apt-get install -y intel-oneapi-common-vars
      
        sudo apt-get install -y intel-oneapi-compiler-dpcpp-cpp-and-cpp-classic
        sudo apt-get install -y intel-oneapi-mkl
               
    - name: Compile
      run: |
        source /opt/intel/oneapi/setvars.sh  --force -intel64
        printenv
        echo "----------------------------oneapi---------------"
        ls /opt/intel/oneapi/
        echo "-------------oneapimkl"
        ls /opt/intel/oneapi/mkl
        echo "-----------End of oneapi_mkl ---------------"
        ls /opt/intel/oneapi/mkl/latest
        echo "-----------End of oneapi_mkl_latest---------------"
        ls /opt/intel/oneapi/mkl/2022.0.2
        echo "---------End of oneapi_mkl_2022.0.2 START of cpath-----------------"
        ls $CPATH
        echo "--------------------------"
        printenv >> $GITHUB_ENV
        echo $GITHUB_ENV
        make -f Makefile.intel

    - name: "Test the code"
      run: |
        echo "Running the tests using the Intel oneapi fortran compiler"
        cd tests/test01
        mpirun -np 1  $HOME/work/reponame/reponame/mpitest
        ./$HOME/work/reponame/reponame/test_program


